datasource db {
  provider = "postgresql"
  url      = "postgresql://neondb_owner:PjJF9DwB5iYp@ep-tiny-waterfall-a59nr6sv.us-east-2.aws.neon.tech/neondb?sslmode=require"
}

generator client {
  provider = "prisma-client-js"
}

model Command {
  id        Int      @id @default(autoincrement())
  command   String
  createdAt DateTime @default(now())
}
